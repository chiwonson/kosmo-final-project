<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
          "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
          
<mapper namespace = "BreadTour.models.Product.mapper.ProductDAO">

<!-- 상품 전체 목록 -->
<select id="listProduct" parameterType="pvo" resultType="pvo">

	SELECT
        product_num AS prnum,
        product_id AS prid,
        product_name AS prname,
        product_cnt AS prcnt,
        product_price AS prprice,
    FROM B_PRBOARD
    ORDER BY product_name
	
</select>

<!-- 상품 상세보기 -->
<select id="detailProduct" parameterType="pvo" resultType="pvo">

	SELECT
        product_num AS prnum,
        product_id AS prid,
        product_name AS prname,
        product_cnt AS prcnt,
        product_price AS prprice,
    FROM B_PRBOARD
    WHERE product_num = #{prnum}
	
</select>

</mapper>

